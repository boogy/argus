//! Wires/unwires llm-monitor into installed tools (Claude Code hooks,
//! opencode plugin, codex config). Stub until Task 14.

pub fn run(_dry_run: bool) -> anyhow::Result<()> {
    Ok(())
}

pub fn uninstall() -> anyhow::Result<()> {
    Ok(())
}
